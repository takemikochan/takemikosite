# Strapi 未設定時はモックデータにフォールバックする（§3.4 の非対称性）。
# ローカルで Strapi に接続する場合のみ、以下2つを設定する。
STRAPI_URL=
STRAPI_TOKEN=

# GA4 測定 ID。未設定時は GA4 タグを出力しない。
PUBLIC_GA_ID=

# お問い合わせフォーム（Cloudflare Worker）。どちらか未設定なら /contact/ はフォールバック表示になる。
PUBLIC_CONTACT_ENDPOINT=
PUBLIC_TURNSTILE_SITE_KEY=
