# Strapi 管理画面ログインの失敗を検知するフィルタ
# nginx の combined ログ形式（cms.takemiko.com のアクセスログ）を対象にする。
# ログイン失敗時、Strapi は 400 を返す。
# 実パスは /admin/login（Strapi 5 の admin プラグインルート定義で確認済み。
# /admin/auth/login ではない）。
[Definition]
failregex = ^<HOST> -.*"POST /admin/login HTTP/\d\.\d" 4\d\d
ignoreregex =
