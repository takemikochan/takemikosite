# Strapi 管理画面ログインの失敗を検知するフィルタ
# nginx の combined ログ形式（cms.takemiko.com のアクセスログ）を対象にする。
# ログイン失敗時、Strapi は 400 を返す。
[Definition]
failregex = ^<HOST> -.*"POST /admin/auth/login HTTP/\d\.\d" 4\d\d
ignoreregex =
